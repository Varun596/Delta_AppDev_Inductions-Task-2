<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="wrap_content"
    android:layout_height="wrap_content" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <ListView
        android:layout_width="wrap_content"
        android:layout_height="250dp"
        android:id="@+id/ContactsListView"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/ToggleSortButton" />

    <ToggleButton
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:id="@+id/ToggleSortButton"
        android:textOff="SORT IN ASCENDING"
        android:textOn="SORT IN DESCENDING"
        android:textSize="25dp"
        android:layout_above="@+id/SearchText"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="250dp"
        android:layout_height="50dp"
        android:id="@+id/SearchText"
        android:layout_alignBaseline="@+id/SearchButton"
        android:layout_alignBottom="@+id/SearchButton"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SEARCH"
        android:id="@+id/SearchButton"
        android:textSize="25dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="50dp" />

</RelativeLayout>
